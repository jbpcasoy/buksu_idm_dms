import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#152033",
    },
    secondary: {
      main: "#F2C050",
    },
  },
});

export default theme;
